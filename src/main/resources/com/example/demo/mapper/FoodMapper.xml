<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.FoodMapper">
    <insert id="insertFood">
        insert into food(foodName, manufacturer, amountPerServings, amountPerCalorie, material)
        values (#{foodName}, #{manufacturer}, #{amountPerServings}, #{amountPerCalorie}, #{material})
    </insert>

    <select id="allView" resultType="Food">
        select *
        from food;
    </select>

</mapper>